<?php

declare(strict_types=1);

return [
    /*
     |--------------------------------------------------------------------------
     | Kafka Brokers
     |--------------------------------------------------------------------------
     */
    'brokers' => env('KAFKA_BROKERS', 'kafka:9092'),

    /*
     |--------------------------------------------------------------------------
     | Security Protocol
     |--------------------------------------------------------------------------
     */
    'securityProtocol' => env('KAFKA_SECURITY_PROTOCOL', 'PLAINTEXT'),

    /*
     |--------------------------------------------------------------------------
     | SASL Configuration
     |--------------------------------------------------------------------------
     */
    'sasl' => [
        'mechanisms' => env('KAFKA_SASL_MECHANISMS', 'PLAIN'),
        'username' => env('KAFKA_SASL_USERNAME', null),
        'password' => env('KAFKA_SASL_PASSWORD', null),
    ],

    /*
     |--------------------------------------------------------------------------
     | Consumer Group ID
     |--------------------------------------------------------------------------
     */
    'consumer_group_id' => env('KAFKA_CONSUMER_GROUP_ID', 'notifications-group'),

    /*
     |--------------------------------------------------------------------------
     | Consumer Timeout (milliseconds)
     |--------------------------------------------------------------------------
     */
    'consumer_timeout_ms' => env('KAFKA_CONSUMER_DEFAULT_TIMEOUT', 10000),

    /*
     |--------------------------------------------------------------------------
     | Offset Reset
     |--------------------------------------------------------------------------
     | earliest - читать с самого начала
     | latest   - только новые сообщения
     */
    'offset_reset' => env('KAFKA_OFFSET_RESET', 'earliest'),

    /*
     |--------------------------------------------------------------------------
     | Auto Commit
     |--------------------------------------------------------------------------
     | false - ручное подтверждение (exactly-once)
     | true  - автоматическое (at-least-once)
     */
    'auto_commit' => env('KAFKA_AUTO_COMMIT', false),

    /*
     |--------------------------------------------------------------------------
     | Sleep on Error (seconds)
     |--------------------------------------------------------------------------
     */
    'sleep_on_error' => env('KAFKA_ERROR_SLEEP', 5),

    /*
     |--------------------------------------------------------------------------
     | Default Partition
     |--------------------------------------------------------------------------
     */
    'partition' => env('KAFKA_PARTITION', 0),

    /*
     |--------------------------------------------------------------------------
     | Compression Type
     |--------------------------------------------------------------------------
     */
    'compression' => env('KAFKA_COMPRESSION_TYPE', 'snappy'),

    /*
     |--------------------------------------------------------------------------
     | Debug Mode
     |--------------------------------------------------------------------------
     */
    'debug' => env('KAFKA_DEBUG', false),

    /*
     |--------------------------------------------------------------------------
     | Flush Settings
     |--------------------------------------------------------------------------
     */
    'flush_retry_sleep_in_ms' => 100,
    'flush_retries' => 10,
    'flush_timeout_in_ms' => 1000,

    /*
     |--------------------------------------------------------------------------
     | Cache Driver
     |--------------------------------------------------------------------------
     */
    'cache_driver' => env('KAFKA_CACHE_DRIVER', env('CACHE_DRIVER', 'redis')),

    /*
     |--------------------------------------------------------------------------
     | Message ID Key
     |--------------------------------------------------------------------------
     */
    'message_id_key' => env('KAFKA_MESSAGE_ID_KEY', 'message_id'),
];
